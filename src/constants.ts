export enum Assets {
  CARDS = 'CARDS',
  TEXTURES = 'TEXTURES',
  MAP = 'MAP',
  HERO = 'HERO',
  DIALOG = 'DIALOG',
  FONT = 'FONT'
}

export enum Direction {
  LEFT,
  RIGHT,
  UP,
  DOWN
}

export const BLOCK_SIZE = 64;
export const TEXTURE_COLUMNS = 16;
