{
  "name": "fit-game",
  "scripts": {
    "start": "node ./deploy.js && tsc --noEmit && parcel view/index.html --out-dir build",
    "deploy": "tsc --noEmit && parcel build view/index.html --out-dir build --no-cache --no-source-maps",
    "lint": "tslint --project tslint.json"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-json-url-loader": "^0.1.2",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "pixi-sound": "^3.0.3",
    "pixi.js": "^5.1.3"
  }
}
